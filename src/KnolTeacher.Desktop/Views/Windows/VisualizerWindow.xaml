<Window x:Class="KnolTeacher.Desktop.Views.Windows.VisualizerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="📷 스마트 실물화상기 (Knol Visualizer)" Height="700" Width="1000"
        WindowStartupLocation="Manual"
        Background="#0F172A">
    <Grid>
        <Grid.RowDefinitions>
            <!-- Top Toolbar -->
            <RowDefinition Height="50" />
            <!-- Camera Viewport -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Controls -->
        <Border Grid.Row="0" Background="#1E293B" BorderBrush="#334155" BorderThickness="0,0,0,1" Padding="12,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Left: Device Selector -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📷 장치:" Foreground="White" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox x:Name="CbCameras" Width="180" SelectionChanged="CbCameras_SelectionChanged" Margin="0,0,10,0"/>
                    <Button Content="🔄 새로고침" Click="BtnRefresh_Click" Padding="8,4" Background="#334155" Foreground="White" BorderThickness="0"/>
                </StackPanel>

                <!-- Center: Video Control Actions -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button Content="🔄 90° 회전" Click="BtnRotate_Click" Margin="0,0,6,0" Padding="8,4" Background="#334155" Foreground="White" BorderThickness="0"/>
                    <Button Content="↔ 좌우반전" Click="BtnFlipH_Click" Margin="0,0,6,0" Padding="8,4" Background="#334155" Foreground="White" BorderThickness="0"/>
                    <Button Content="↕ 상하반전" Click="BtnFlipV_Click" Margin="0,0,6,0" Padding="8,4" Background="#334155" Foreground="White" BorderThickness="0"/>
                    
                    <ToggleButton x:Name="BtnFreeze" Content="⏸ 정지" Checked="BtnFreeze_Checked" Unchecked="BtnFreeze_Unchecked" Margin="0,0,6,0" Padding="8,4" Background="#334155" Foreground="White" BorderThickness="0"/>
                    <ToggleButton x:Name="BtnDocEnhance" Content="📄 문서강조" Checked="BtnDocEnhance_Checked" Unchecked="BtnDocEnhance_Unchecked" Margin="0,0,10,0" Padding="8,4" Background="#334155" Foreground="White" BorderThickness="0"/>

                    <Button Content="📸 스냅샷 캡처" Click="BtnSnapshot_Click" Padding="10,4" Background="#0284C7" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
                </StackPanel>

                <!-- Right: Status / Close -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BtnSwitchMonitor" Content="📺 모니터 2" Click="BtnSwitchMonitor_Click" Padding="8,4" Margin="0,0,10,0" Background="#1E293B" Foreground="#38BDF8" BorderBrush="#0284C7" BorderThickness="1" FontSize="11" FontWeight="Bold" Cursor="Hand" ToolTip="실물화상기 창을 모니터 1 ↔ 모니터 2로 즉시 이동"/>
                    <TextBlock x:Name="TxtStatus" Text="대기 중" Foreground="#38BDF8" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <Button Content="✕ 닫기" Click="BtnClose_Click" Padding="10,4" Background="#DC2626" Foreground="White" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Video Viewport -->
        <Border Grid.Row="1" Background="Black" ClipToBounds="True">
            <Image x:Name="CameraImage" Stretch="Uniform" />
        </Border>
    </Grid>
</Window>
