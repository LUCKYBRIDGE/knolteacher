<Window x:Class="KnolTeacher.Desktop.Views.Windows.AvatarPickerModalDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="🐾 동물 얼굴 아바타 선택" Height="620" Width="680"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="#0F172A">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <!-- Header -->
            <RowDefinition Height="Auto" />
            <!-- Avatar Gallery (Scrollable Grid) -->
            <RowDefinition Height="*" />
            <!-- Footer -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Margin="0,0,0,14" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="12" Padding="14,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🐾" FontSize="18" Margin="0,0,8,0"/>
                        <TextBlock x:Name="TxtHeaderTitle" Text="학생 동물 아바타 선택 (32종)" FontWeight="Bold" FontSize="16" Foreground="White"/>
                    </StackPanel>
                    <TextBlock x:Name="TxtStudentDesc" Text="원하는 동물 얼굴을 클릭하면 학생의 고유 아바타로 등록됩니다." FontSize="12" Foreground="#94A3B8" Margin="0,4,0,0"/>
                </StackPanel>
                <Border Grid.Column="1" Background="#0F172A" CornerRadius="28" BorderBrush="#38BDF8" BorderThickness="2" Width="56" Height="56">
                    <Image x:Name="ImgCurrentAvatar" Width="48" Height="48" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>

        <!-- Gallery -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,14">
            <ItemsControl x:Name="ItemsAvatars">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="4" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="6" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="10" Padding="8" Cursor="Hand" MouseLeftButtonUp="AvatarCard_Click">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#334155"/>
                                            <Setter Property="BorderBrush" Value="#38BDF8"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Image Source="{Binding ImageUri}" Width="68" Height="68" Margin="0,0,0,6" RenderOptions.BitmapScalingMode="HighQuality"/>
                                <TextBlock Text="{Binding NameKo}" FontWeight="Bold" FontSize="13" Foreground="White" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer -->
        <Grid Grid.Row="2">
            <Button Content="닫기" Click="BtnClose_Click" HorizontalAlignment="Right" Width="90" Height="36" Background="#334155" Foreground="White" FontWeight="Bold" FontSize="13" BorderThickness="0" Cursor="Hand">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
            </Button>
        </Grid>
    </Grid>
</Window>
