<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.MealWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="TxtMeal" Text="급식 메뉴 불러오는 중..." FontSize="12" LineHeight="20" Foreground="White" FontWeight="SemiBold" TextWrapping="Wrap"/>
        </ScrollViewer>

        <Border Grid.Row="1" Background="#0F172A" CornerRadius="6" Padding="8,4" Margin="0,6,0,0">
            <TextBlock x:Name="TxtCalorie" Text="열량: 계산 중" FontSize="11" Foreground="#38BDF8" HorizontalAlignment="Right" FontWeight="Bold"/>
        </Border>
    </Grid>
</UserControl>
