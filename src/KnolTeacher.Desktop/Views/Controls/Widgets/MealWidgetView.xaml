<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.MealWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <!-- Date & Calorie Nav Header -->
            <RowDefinition Height="Auto" />
            <!-- Menu Items Scroll -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Date Navigation & Calorie Pill -->
        <Border Grid.Row="0" Background="#0F172A" CornerRadius="8" BorderBrush="#334155" BorderThickness="1" Padding="8,5" Margin="0,0,0,8">
            <DockPanel LastChildFill="True">
                <!-- Right Calorie Pill -->
                <Border DockPanel.Dock="Right" Background="#1E293B" CornerRadius="10" BorderBrush="#38BDF8" BorderThickness="1" Padding="8,2" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtCalorie" Text="720 kcal" FontSize="11" Foreground="#38BDF8" FontWeight="Bold"/>
                </Border>

                <!-- Left Date Navigation -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="‹" Click="BtnPrevMeal_Click" Width="22" Height="22" Padding="0" Background="#1E293B" Foreground="#CBD5E1" BorderBrush="#334155" BorderThickness="1" FontSize="13" FontWeight="Bold" Cursor="Hand">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="5"/></Style></Button.Resources>
                    </Button>
                    <TextBlock x:Name="TxtMealDate" Text="오늘 (9.9 수)" FontSize="11.5" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="8,0"/>
                    <Button Content="›" Click="BtnNextMeal_Click" Width="22" Height="22" Padding="0" Background="#1E293B" Foreground="#CBD5E1" BorderBrush="#334155" BorderThickness="1" FontSize="13" FontWeight="Bold" Cursor="Hand">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="5"/></Style></Button.Resources>
                    </Button>
                    <Button Content="오늘" Click="BtnTodayMeal_Click" Padding="6,2" Margin="6,0,0,0" Background="#1E293B" Foreground="#38BDF8" BorderBrush="#0284C7" BorderThickness="1" FontSize="10.5" FontWeight="Bold" Cursor="Hand">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="5"/></Style></Button.Resources>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Menu Container Box -->
        <Border Grid.Row="1" Background="#0F172A" CornerRadius="8" BorderBrush="#334155" BorderThickness="1" Padding="12,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="TxtMeal" Text="급식 메뉴 불러오는 중..." FontSize="13" LineHeight="22" Foreground="#F8FAFC" FontWeight="SemiBold" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
