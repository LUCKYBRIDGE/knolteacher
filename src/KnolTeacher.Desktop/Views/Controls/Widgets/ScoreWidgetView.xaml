<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.ScoreWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <!-- Top Controls (Group Count Selector) -->
            <RowDefinition Height="Auto" />
            <!-- Dynamic Grid of Groups -->
            <RowDefinition Height="*" />
            <!-- Bottom Action Buttons Bar -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Top Header & Group Count Selector -->
        <Border Grid.Row="0" Background="#0F172A" CornerRadius="8" BorderBrush="#334155" BorderThickness="1" Padding="8,4" Margin="0,0,0,6">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="모둠 수: " FontSize="11" Foreground="#94A3B8" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <ComboBox x:Name="CbGroupCount" Width="76" Height="24" FontSize="11" Background="#1E293B" Foreground="White" BorderBrush="#334155" VerticalContentAlignment="Center" SelectionChanged="CbGroupCount_SelectionChanged">
                        <ComboBoxItem Content="2모둠" Tag="2"/>
                        <ComboBoxItem Content="3모둠" Tag="3"/>
                        <ComboBoxItem Content="4모둠" Tag="4"/>
                        <ComboBoxItem Content="5모둠" Tag="5"/>
                        <ComboBoxItem Content="6모둠" Tag="6" IsSelected="True"/>
                        <ComboBoxItem Content="7모둠" Tag="7"/>
                        <ComboBoxItem Content="8모둠" Tag="8"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🏆 " FontSize="12" VerticalAlignment="Center"/>
                    <TextBlock Text="모둠 칭찬 점수판" FontSize="11.5" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Dynamic UniformGrid Container for Groups -->
        <UniformGrid x:Name="GridGroups" Grid.Row="1" Margin="0,0,0,6"/>

        <!-- Bottom Action Bar -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="⭐ 전원 +1점 부여" Click="BtnAddAll_Click" Height="28" Margin="0,0,3,0" Background="#0369A1" Foreground="White" FontSize="11" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
            </Button>
            <Button Grid.Column="1" Content="🔄 점수 전체 초기화" Click="BtnReset_Click" Height="28" Margin="3,0,0,0" Background="#334155" Foreground="#CBD5E1" FontSize="11" BorderThickness="0" Cursor="Hand">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
            </Button>
        </Grid>
    </Grid>
</UserControl>
