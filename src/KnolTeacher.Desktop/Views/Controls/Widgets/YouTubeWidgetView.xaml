<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.YouTubeWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <!-- Playlist selector -->
            <RowDefinition Height="Auto" />
            <!-- Billboard Display -->
            <RowDefinition Height="*" />
            <!-- Controls Bar -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Playlist Selector -->
        <ComboBox x:Name="CbTracks" Grid.Row="0" Height="28" MinHeight="28" FontSize="11" Padding="6,2" VerticalContentAlignment="Center" Margin="0,0,0,6" SelectionChanged="CbTracks_SelectionChanged"/>

        <!-- Display Card -->
        <Border Grid.Row="1" Background="#0F172A" CornerRadius="8" BorderBrush="#334155" BorderThickness="1" Margin="0,0,0,6" Padding="8">
            <Grid VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="🎵 무광고 교실 BGM" FontSize="11" Foreground="#38BDF8" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                <Viewbox Grid.Row="1" Stretch="Uniform" MaxHeight="45">
                    <TextBlock x:Name="TxtTitle" Text="음악을 선택하세요" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                </Viewbox>
                <TextBlock Grid.Row="2" x:Name="TxtStatus" Text="구간 반복 및 루프 지원" FontSize="10" Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,4,0,0"/>
            </Grid>
        </Border>

        <!-- Controls -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button x:Name="BtnOpenPlayer" Grid.Column="0" Content="📺 전체 플레이어 / 구간반복 열기" Click="BtnOpenPlayer_Click" Height="30" Margin="0,0,4,0" Background="#0284C7" Foreground="White" FontSize="11" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
            </Button>
            <Button Grid.Column="1" Content="➕ 링크" Click="BtnAdd_Click" Height="30" Padding="8,0" Background="#1E293B" Foreground="#38BDF8" FontSize="11" FontWeight="Bold" BorderThickness="0" Cursor="Hand" ToolTip="새 유튜브 링크 등록">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
            </Button>
        </Grid>
    </Grid>
</UserControl>
