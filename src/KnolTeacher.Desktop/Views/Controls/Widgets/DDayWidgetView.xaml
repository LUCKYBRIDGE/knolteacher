<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.DDayWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <!-- Title Bar -->
            <RowDefinition Height="Auto"/>
            <!-- Main Hero D-Day Counter -->
            <RowDefinition Height="*"/>
            <!-- Action Bar -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Title Bar -->
        <Border Grid.Row="0" Background="#0F172A" CornerRadius="8" BorderBrush="#334155" BorderThickness="1" Padding="10,6" Margin="0,0,0,8">
            <DockPanel LastChildFill="True">
                <Border DockPanel.Dock="Right" Background="#1E293B" CornerRadius="10" BorderBrush="#F59E0B" BorderThickness="1" Padding="8,2" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtBadgeStatus" Text="진행 중" FontSize="10.5" Foreground="#FDE047" FontWeight="Bold"/>
                </Border>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🎯 " FontSize="13" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TxtDDayTitle" Text="여름방학" FontSize="13" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Big Neon D-Day Counter in Viewbox -->
        <Border Grid.Row="1" Background="#0F172A" CornerRadius="10" BorderBrush="#334155" BorderThickness="1" Padding="14,10" Margin="0,0,0,8">
            <Viewbox Stretch="Uniform">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtDDayCount" Text="D-14" FontFamily="Consolas, Malgun Gothic" FontSize="52" FontWeight="Bold" Foreground="#F59E0B" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="TxtDDayTarget" Text="목표일: 2026-07-24 (금)" FontSize="12.5" FontWeight="SemiBold" Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    <TextBlock x:Name="TxtDDaySub" Text="학급 공통 목표일까지 14일 남았습니다" FontSize="11" Foreground="#38BDF8" HorizontalAlignment="Center" Margin="0,3,0,0"/>
                </StackPanel>
            </Viewbox>
        </Border>

        <!-- Bottom Action Bar -->
        <Grid Grid.Row="2">
            <Button Content="🎯 목표일 및 행사명 변경..." Click="BtnChangeDDay_Click" Height="30" Background="#1E293B" Foreground="#38BDF8" BorderBrush="#0284C7" BorderThickness="1" FontSize="11" FontWeight="Bold" Cursor="Hand">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
            </Button>
        </Grid>
    </Grid>
</UserControl>
