<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.TimetableWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <!-- Header notice -->
            <RowDefinition Height="Auto" />
            <!-- Timetable List -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Status / Help -->
        <Border Grid.Row="0" Background="#0F172A" CornerRadius="6" BorderBrush="#334155" BorderThickness="1" Padding="8,4" Margin="0,0,0,6">
            <Grid>
                <TextBlock Text="💡 ✏️ 버튼을 눌러 과목을 수정하면 메인 앱과 즉시 연동됩니다." FontSize="10" Foreground="#94A3B8" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ListItems">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="6" Padding="8,5" Margin="0,0,0,4">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#0F172A"/>
                                    <Setter Property="BorderBrush" Value="#334155"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsCurrentPeriod}" Value="True">
                                            <Setter Property="Background" Value="#0369A1"/>
                                            <Setter Property="BorderBrush" Value="#38BDF8"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding DisplayBadge}" FontWeight="Bold" FontSize="11" Foreground="#38BDF8" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding Subject}" FontWeight="Bold" FontSize="12" Foreground="White" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Grid.Column="2" Text="{Binding Start}" FontSize="10" Foreground="#94A3B8" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <Button Grid.Column="3" Content="✏️" Click="BtnEdit_Click" Tag="{Binding}" Width="24" Height="22" Padding="0" Background="#1E293B" Foreground="#38BDF8" BorderThickness="0" Cursor="Hand" ToolTip="과목명 수정 (메인 시간표와 자동 연동)"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
