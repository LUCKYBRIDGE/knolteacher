<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.TimetableWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <!-- Header notice -->
            <RowDefinition Height="Auto" />
            <!-- Timetable List -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Status / Help -->
        <Border Grid.Row="0" Background="#0F172A" CornerRadius="6" BorderBrush="#334155" BorderThickness="1" Padding="8,4" Margin="0,0,0,6">
            <Grid>
                <TextBlock Text="💡 ✏️ 버튼을 눌러 과목을 수정하면 메인 앱과 즉시 연동됩니다." FontSize="10" Foreground="#94A3B8" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ListItems">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="6" Padding="8,5" Margin="0,0,0,4" Cursor="Hand" ToolTip="우클릭하여 과목명을 빠르게 수정할 수 있습니다">
                            <Border.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="✏️ 과목명 직접 수정..." Click="MenuEditSubject_Click"/>
                                    <Separator/>
                                    <MenuItem Header="📚 빠른 과목 변경">
                                        <MenuItem Header="국어" Click="MenuQuickSubject_Click" Tag="국어"/>
                                        <MenuItem Header="수학" Click="MenuQuickSubject_Click" Tag="수학"/>
                                        <MenuItem Header="사회" Click="MenuQuickSubject_Click" Tag="사회"/>
                                        <MenuItem Header="과학" Click="MenuQuickSubject_Click" Tag="과학"/>
                                        <MenuItem Header="영어" Click="MenuQuickSubject_Click" Tag="영어"/>
                                        <MenuItem Header="체육" Click="MenuQuickSubject_Click" Tag="체육"/>
                                        <MenuItem Header="음악" Click="MenuQuickSubject_Click" Tag="음악"/>
                                        <MenuItem Header="미술" Click="MenuQuickSubject_Click" Tag="미술"/>
                                        <MenuItem Header="도덕" Click="MenuQuickSubject_Click" Tag="도덕"/>
                                        <MenuItem Header="실과" Click="MenuQuickSubject_Click" Tag="실과"/>
                                        <Separator/>
                                        <MenuItem Header="창체 (창의적 체험활동)" Click="MenuQuickSubject_Click" Tag="창체"/>
                                        <MenuItem Header="동아리" Click="MenuQuickSubject_Click" Tag="동아리"/>
                                        <MenuItem Header="자율 / 안전" Click="MenuQuickSubject_Click" Tag="자율"/>
                                        <MenuItem Header="진로활동" Click="MenuQuickSubject_Click" Tag="진로"/>
                                    </MenuItem>
                                    <MenuItem Header="🏷️ 수업 구분(태그) 설정">
                                        <MenuItem Header="🏫 교실 수업" Click="MenuQuickTag_Click" Tag="교실수업"/>
                                        <MenuItem Header="🎒 이동 수업" Click="MenuQuickTag_Click" Tag="이동수업"/>
                                        <Separator/>
                                        <MenuItem Header="태그 없음" Click="MenuQuickTag_Click" Tag=""/>
                                    </MenuItem>
                                    <Separator/>
                                    <MenuItem Header="🧹 빈 시간으로 비우기" Click="MenuClearSubject_Click"/>
                                </ContextMenu>
                            </Border.ContextMenu>
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#0F172A"/>
                                    <Setter Property="BorderBrush" Value="#334155"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsCurrentPeriod}" Value="True">
                                            <Setter Property="Background" Value="#075985"/>
                                            <Setter Property="BorderBrush" Value="#38BDF8"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="52" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding DisplayBadge}" FontWeight="Bold" FontSize="11.5" Foreground="#38BDF8" VerticalAlignment="Center"/>
                                <DockPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,6,0">
                                    <Border DockPanel.Dock="Right" Margin="6,0,0,0" CornerRadius="4" Padding="5,1" Background="#0284C7" Visibility="{Binding IsCurrentPeriod, Converter={StaticResource BoolToVisConverter}}" VerticalAlignment="Center">
                                        <TextBlock Text="현재 수업" FontSize="9.5" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                    <TextBlock Text="{Binding Subject}" FontWeight="Bold" FontSize="12.5" Foreground="White" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                </DockPanel>
                                <TextBlock Grid.Column="2" Text="{Binding TimeRange}" FontSize="10" Foreground="#94A3B8" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <Button Grid.Column="3" Content="✏️" Click="BtnEdit_Click" Tag="{Binding}" Width="24" Height="22" Padding="0" Background="#1E293B" Foreground="#38BDF8" BorderThickness="0" Cursor="Hand" ToolTip="과목명 수정 (메인 시간표와 자동 연동)"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
