<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.PickerWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <!-- Top Filter Bar -->
            <RowDefinition Height="Auto" />
            <!-- Billboard Result -->
            <RowDefinition Height="*" />
            <!-- Pick Action Button -->
            <RowDefinition Height="Auto" />
            <!-- History Text -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Mode & Filter & Manual Range Bar -->
        <Grid Grid.Row="0" Margin="0,0,0,6">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Line 1: Mode & Gender -->
            <Grid Grid.Row="0" Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <RadioButton x:Name="RbName" Content="이름" IsChecked="True" Foreground="White" FontSize="11" Margin="0,0,6,0" Checked="Filter_Changed"/>
                    <RadioButton x:Name="RbNumber" Content="번호" Foreground="White" FontSize="11" Checked="Filter_Changed"/>
                </StackPanel>
                <ComboBox x:Name="CbGender" Grid.Column="1" Height="30" MinHeight="30" FontSize="11" Padding="6,2" VerticalContentAlignment="Center" SelectionChanged="Filter_Changed" HorizontalAlignment="Right" Width="95">
                    <ComboBoxItem Content="전체" IsSelected="True"/>
                    <ComboBoxItem Content="👦 남학생"/>
                    <ComboBoxItem Content="👧 여학생"/>
                </ComboBox>
            </Grid>

            <!-- Line 2: Manual Number Range Input -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="번호 직접입력:" Foreground="#94A3B8" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <TextBox x:Name="TbStartNum" Text="1" Width="28" Height="24" FontSize="11" FontWeight="Bold" TextAlignment="Center" Background="#1E293B" Foreground="White" BorderBrush="#334155" VerticalContentAlignment="Center" TextChanged="Range_TextChanged"/>
                <TextBlock Text="~" Foreground="#94A3B8" FontSize="11" VerticalAlignment="Center" Margin="2,0"/>
                <TextBox x:Name="TbEndNum" Text="25" Width="30" Height="24" FontSize="11" FontWeight="Bold" TextAlignment="Center" Background="#1E293B" Foreground="White" BorderBrush="#334155" VerticalContentAlignment="Center" TextChanged="Range_TextChanged"/>
                <TextBlock Text="번까지" Foreground="#94A3B8" FontSize="10" VerticalAlignment="Center" Margin="3,0,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Big Display Card (Responsive Viewbox) -->
        <Border Grid.Row="1" Background="#0F172A" CornerRadius="10" BorderBrush="#334155" BorderThickness="1" Margin="0,0,0,6" Padding="8">
            <Viewbox Stretch="Uniform">
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border x:Name="BorderWinnerAvatar" Width="72" Height="72" CornerRadius="36" BorderBrush="#FBBF24" BorderThickness="3" HorizontalAlignment="Center" Margin="0,0,0,6" Visibility="Collapsed">
                        <Border.Background>
                            <ImageBrush x:Name="ImgWinnerAvatar" Stretch="UniformToFill"/>
                        </Border.Background>
                    </Border>
                    <TextBlock x:Name="TxtWinner" Text="?" FontFamily="Malgun Gothic" FontSize="52" FontWeight="Bold" Foreground="#38BDF8" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
            </Viewbox>
        </Border>

        <!-- Action Button -->
        <Button x:Name="BtnPick" Grid.Row="2" Content="🎲 추첨하기!" Click="BtnPick_Click" Height="36" Margin="0,0,0,6" Background="#0284C7" Foreground="White" FontWeight="Bold" FontSize="14" BorderThickness="0" Cursor="Hand">
            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
        </Button>

        <!-- History -->
        <ScrollViewer Grid.Row="3" MaxHeight="50" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="TxtHistory" Text="기록: 없음" FontSize="10" Foreground="#94A3B8" TextWrapping="Wrap"/>
        </ScrollViewer>
    </Grid>
</UserControl>
