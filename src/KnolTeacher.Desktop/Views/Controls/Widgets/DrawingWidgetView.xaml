<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.DrawingWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <!-- Mini Tool Bar -->
            <RowDefinition Height="Auto" />
            <!-- Ink Surface -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Mini Toolbar -->
        <Border Grid.Row="0" Background="#0F172A" CornerRadius="6" BorderBrush="#334155" BorderThickness="1" Padding="4,3" Margin="0,0,0,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BtnModeGreen" Content="🏫 칠판" Click="BtnModeGreen_Click" Height="24" Padding="6,0" Margin="0,0,3,0" Background="#1B4332" Foreground="White" FontSize="10" FontWeight="Bold"/>
                    <Button x:Name="BtnModeWhite" Content="📋 화이트" Click="BtnModeWhite_Click" Height="24" Padding="6,0" Margin="0,0,6,0" Background="#F8F9FA" Foreground="#0F172A" FontSize="10" FontWeight="Bold"/>

                    <RadioButton x:Name="RbPen" Content="펜" IsChecked="True" Checked="RbPen_Checked" Foreground="White" FontSize="10" Margin="0,0,4,0" VerticalAlignment="Center"/>
                    <RadioButton x:Name="RbEraser" Content="지우개" Checked="RbEraser_Checked" Foreground="White" FontSize="10" Margin="0,0,6,0" VerticalAlignment="Center"/>

                    <!-- Colors -->
                    <Button Width="16" Height="16" Background="White" Click="BtnColor_Click" Tag="#FFFFFF" Margin="0,0,2,0" Cursor="Hand" BorderThickness="0"/>
                    <Button Width="16" Height="16" Background="#FDE047" Click="BtnColor_Click" Tag="#FDE047" Margin="0,0,2,0" Cursor="Hand" BorderThickness="0"/>
                    <Button Width="16" Height="16" Background="#F472B6" Click="BtnColor_Click" Tag="#F472B6" Margin="0,0,2,0" Cursor="Hand" BorderThickness="0"/>
                    <Button Width="16" Height="16" Background="#38BDF8" Click="BtnColor_Click" Tag="#38BDF8" Margin="0,0,6,0" Cursor="Hand" BorderThickness="0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="↩" Click="BtnUndo_Click" Height="24" Padding="6,0" Margin="0,0,3,0" Background="#334155" Foreground="White" FontSize="10" ToolTip="실행 취소"/>
                    <Button Content="🗑" Click="BtnClear_Click" Height="24" Padding="6,0" Background="#DC2626" Foreground="White" FontSize="10" ToolTip="전체 지우기"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Ink Drawing Canvas Surface -->
        <Border Grid.Row="1" x:Name="InkBorder" Background="#1B4332" CornerRadius="8" BorderBrush="#334155" BorderThickness="1" ClipToBounds="True">
            <InkCanvas x:Name="MiniInkCanvas" Background="Transparent"/>
        </Border>
    </Grid>
</UserControl>
