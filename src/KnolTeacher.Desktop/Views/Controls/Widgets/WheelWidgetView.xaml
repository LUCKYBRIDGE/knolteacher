<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.WheelWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <!-- Preset & Edit Mode Bar -->
            <RowDefinition Height="Auto" />
            <!-- Custom Input Area (Collapsible) -->
            <RowDefinition Height="Auto" />
            <!-- Display -->
            <RowDefinition Height="*" />
            <!-- Action -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Top Presets Bar -->
        <Grid Grid.Row="0" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="CbPresets" Grid.Column="0" Height="28" MinHeight="28" FontSize="11" Padding="6,2" VerticalContentAlignment="Center" SelectionChanged="CbPresets_SelectionChanged">
                <ComboBoxItem Content="👥 1~6 모둠" IsSelected="True"/>
                <ComboBoxItem Content="🎯 1~25번 학생"/>
                <ComboBoxItem Content="📚 수업 교과목"/>
                <ComboBoxItem Content="🧹 학급 1인1역/청소"/>
                <ComboBoxItem Content="✏️ 사용자 직접 입력"/>
            </ComboBox>
            <Button x:Name="BtnToggleEdit" Grid.Column="1" Content="✏️ 항목 편집" Click="BtnToggleEdit_Click" Height="28" Padding="8,0" Margin="4,0,0,0" Background="#1E293B" Foreground="#38BDF8" FontSize="10" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
        </Grid>

        <!-- Custom Items Input (Collapsible) -->
        <Border x:Name="PanelCustomInput" Grid.Row="1" Background="#0F172A" CornerRadius="6" BorderBrush="#334155" BorderThickness="1" Padding="6" Margin="0,0,0,6" Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="쉼표(,) 또는 줄바꿈으로 항목을 구분하여 입력하세요:" FontSize="10" Foreground="#94A3B8" Margin="0,0,0,4"/>
                <TextBox x:Name="TbCustomItems" Height="48" FontSize="11" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Background="#1E293B" Foreground="White" BorderBrush="#334155" Padding="4" TextChanged="TbCustomItems_TextChanged"/>
                <TextBlock x:Name="TxtItemCount" Text="총 6개 항목" FontSize="10" Foreground="#10B981" HorizontalAlignment="Right" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <!-- Result Display Card (Responsive Viewbox) -->
        <Border Grid.Row="2" Background="#0F172A" CornerRadius="10" BorderBrush="#334155" BorderThickness="1" Margin="0,0,0,6" Padding="8">
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="🎡" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,2"/>
                <Viewbox Grid.Row="1" Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Center" MaxHeight="60">
                    <TextBlock x:Name="TxtResult" Text="돌림판을 돌려보세요!" FontSize="20" FontWeight="Bold" Foreground="#38BDF8" HorizontalAlignment="Center"/>
                </Viewbox>
            </Grid>
        </Border>

        <!-- Spin Action Button -->
        <Button x:Name="BtnSpin" Grid.Row="3" Content="🎡 돌리기!" Click="BtnSpin_Click" Height="34" Background="#0284C7" Foreground="White" FontWeight="Bold" FontSize="13" BorderThickness="0" Cursor="Hand">
            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
        </Button>
    </Grid>
</UserControl>
