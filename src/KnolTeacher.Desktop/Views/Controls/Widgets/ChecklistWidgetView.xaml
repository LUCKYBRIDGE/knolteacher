<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.ChecklistWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="380" d:DesignWidth="360">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <!-- 1. Tabs bar -->
            <RowDefinition Height="Auto"/>
            <!-- 2. Status & Quick Actions -->
            <RowDefinition Height="Auto"/>
            <!-- 3. Student Grid -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. Tabs Bar -->
        <Border Grid.Row="0" Background="#0F172A" CornerRadius="6" BorderBrush="#334155" BorderThickness="1" Padding="4,3" Margin="0,0,0,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="PanelTabs" Orientation="Horizontal"/>
                </ScrollViewer>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="4,0,0,0">
                    <Button Content="＋" Click="BtnAddTab_Click" ToolTip="새 체크리스트 탭 추가" Width="24" Height="24" Background="#0284C7" Foreground="White" FontWeight="Bold" FontSize="12" BorderThickness="0" Cursor="Hand">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                    </Button>
                    <Button Content="🗑️" Click="BtnDeleteCurrentTab_Click" ToolTip="현재 탭 삭제" Width="24" Height="24" Margin="3,0,0,0" Background="#334155" Foreground="#CBD5E1" FontSize="10" BorderThickness="0" Cursor="Hand">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 2. Status & Quick Actions -->
        <Border Grid.Row="1" Background="#1E293B" CornerRadius="6" Padding="6,4" Margin="0,0,0,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Status Badge -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border x:Name="BorderPendingBadge" Background="#DC2626" CornerRadius="4" Padding="6,2" Margin="0,0,6,0">
                        <TextBlock x:Name="TxtPendingBadge" Text="미완료 0명" FontWeight="Bold" FontSize="11" Foreground="White"/>
                    </Border>
                    <TextBlock x:Name="TxtTotalRatio" Text="전체 0명" Foreground="#94A3B8" FontSize="11" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="전체체크" Click="BtnCheckAll_Click" Height="22" Padding="6,0" Margin="0,0,3,0" Background="#0F172A" Foreground="#34D399" FontSize="10" FontWeight="Bold" BorderBrush="#334155" BorderThickness="1" Cursor="Hand">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                    </Button>
                    <Button Content="초기화" Click="BtnReset_Click" Height="22" Padding="6,0" Margin="0,0,3,0" Background="#0F172A" Foreground="#F87171" FontSize="10" BorderBrush="#334155" BorderThickness="1" Cursor="Hand">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                    </Button>
                    <Button x:Name="BtnToggleNameMode" Content="이름" Click="BtnToggleNameMode_Click" ToolTip="번호/이름 표시 전환" Height="22" Padding="6,0" Background="#0F172A" Foreground="#38BDF8" FontSize="10" FontWeight="Bold" BorderBrush="#334155" BorderThickness="1" Cursor="Hand">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 3. Student Grid -->
        <Border Grid.Row="2" Background="#0F172A" CornerRadius="6" BorderBrush="#334155" BorderThickness="1" Padding="4">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <WrapPanel x:Name="PanelStudentGrid" Orientation="Horizontal" ItemWidth="58" ItemHeight="42"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
