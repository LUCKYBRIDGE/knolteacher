<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.DiceWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <!-- Settings Bar -->
            <RowDefinition Height="Auto" />
            <!-- Main Content (Dice + Stats Table) -->
            <RowDefinition Height="*" />
            <!-- Action Bar -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Top Options -->
        <Grid Grid.Row="0" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="개수:" Foreground="#94A3B8" FontSize="11" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <RadioButton x:Name="Rb1Dice" Content="1개" IsChecked="True" Foreground="White" FontSize="11" Margin="0,0,6,0" Checked="Config_Changed"/>
                <RadioButton x:Name="Rb2Dice" Content="2개" Foreground="White" FontSize="11" Checked="Config_Changed"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="면수(수기입력):" Foreground="#94A3B8" FontSize="11" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ComboBox x:Name="CbFaces" Width="95" Height="32" MinHeight="32" FontSize="11" Padding="6,2" VerticalContentAlignment="Center" IsEditable="True" SelectionChanged="Config_Changed" LostFocus="Config_Changed" KeyDown="CbFaces_KeyDown">
                    <ComboBoxItem Content="D6 (6면)" IsSelected="True"/>
                    <ComboBoxItem Content="D4 (4면)"/>
                    <ComboBoxItem Content="D8 (8면)"/>
                    <ComboBoxItem Content="D10 (10면)"/>
                    <ComboBoxItem Content="D12 (12면)"/>
                    <ComboBoxItem Content="D20 (20면)"/>
                    <ComboBoxItem Content="D100 (100면)"/>
                </ComboBox>
            </StackPanel>
        </Grid>

        <!-- Center Split: Left Dice & Right Stats -->
        <Grid Grid.Row="1" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left: Big Dice Display in Responsive Viewbox -->
            <Border Grid.Column="0" Background="#0F172A" CornerRadius="8" BorderBrush="#334155" BorderThickness="1" Margin="0,0,6,0" Padding="6">
                <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Viewbox Grid.Row="0" Stretch="Uniform" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtDiceSymbol" Text="⚅" FontFamily="Segoe UI Symbol" FontSize="48" Foreground="#38BDF8" HorizontalAlignment="Center"/>
                    </Viewbox>
                    <TextBlock Grid.Row="1" x:Name="TxtDiceValue" Text="결과: 6" FontSize="12" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </Grid>
            </Border>

            <!-- Right: Real-time Math Statistics Table -->
            <Border Grid.Column="1" Background="#0F172A" CornerRadius="8" BorderBrush="#334155" BorderThickness="1" Padding="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <!-- Header -->
                    <Border Grid.Row="0" Background="#1E293B" CornerRadius="4" Padding="4,2" Margin="0,0,0,2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="28" />
                                <ColumnDefinition Width="38" />
                                <ColumnDefinition Width="46" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="눈" Foreground="#38BDF8" FontSize="10" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="빈도" Foreground="White" FontSize="10" FontWeight="Bold"/>
                            <TextBlock Grid.Column="2" Text="비율" Foreground="#94A3B8" FontSize="10" FontWeight="Bold"/>
                            <TextBlock Grid.Column="3" Text="백분율(%)" Foreground="#10B981" FontSize="10" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                    <!-- Scrollable Table Body -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="ListStats">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="4,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="28" />
                                            <ColumnDefinition Width="38" />
                                            <ColumnDefinition Width="46" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Face}" FontFamily="Consolas" FontWeight="Bold" Foreground="White" FontSize="10"/>
                                        <TextBlock Grid.Column="1" Text="{Binding CountStr}" Foreground="#94A3B8" FontSize="10"/>
                                        <TextBlock Grid.Column="2" Text="{Binding RatioStr}" FontFamily="Consolas" Foreground="#CBD5E1" FontSize="10"/>
                                        <TextBlock Grid.Column="3" Text="{Binding PercentStr}" FontFamily="Consolas" FontWeight="Bold" Foreground="#10B981" FontSize="10" HorizontalAlignment="Right"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Bottom Action Bar -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button x:Name="BtnRoll" Grid.Column="0" Content="🎲 주사위 굴리기!" Click="BtnRoll_Click" Height="34" Margin="0,0,6,0" Background="#0284C7" Foreground="White" FontWeight="Bold" FontSize="13" BorderThickness="0" Cursor="Hand">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
            </Button>
            <Button Content="통계 초기화" Click="BtnReset_Click" Grid.Column="1" Height="34" Padding="8,0" Background="#334155" Foreground="White" FontSize="11" BorderThickness="0" Cursor="Hand">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
            </Button>
        </Grid>
    </Grid>
</UserControl>
