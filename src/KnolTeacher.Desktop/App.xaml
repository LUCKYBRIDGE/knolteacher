<Application x:Class="KnolTeacher.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Wpf.Ui;component/Resources/Theme/Light.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Wpf.Ui;component/Resources/Wpf.Ui.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- 🌾 KnolTeacher Warm Beige Theme Palette -->
            <SolidColorBrush x:Key="BeigeAppBg" Color="#F8F5EE" />
            <SolidColorBrush x:Key="BeigeSidebarBg" Color="#ECE3D4" />
            <SolidColorBrush x:Key="BeigeSidebarHover" Color="#DFD2BE" />
            <SolidColorBrush x:Key="BeigeAccent" Color="#B45309" />
            <SolidColorBrush x:Key="BeigeAccentHover" Color="#92400E" />
            <SolidColorBrush x:Key="BeigeAccentSoft" Color="#FEF3C7" />
            <SolidColorBrush x:Key="BeigeCardBg" Color="#FFFFFF" />
            <SolidColorBrush x:Key="BeigeCardInner" Color="#FCFBF9" />
            <SolidColorBrush x:Key="BeigeCardBorder" Color="#DBCDBA" />
            <SolidColorBrush x:Key="BeigeTextMain" Color="#1C1917" />
            <SolidColorBrush x:Key="BeigeTextSub" Color="#44403C" />
            <SolidColorBrush x:Key="BeigeTextMuted" Color="#78716C" />
            <SolidColorBrush x:Key="BeigeLunchBg" Color="#FBF7F0" />
            <SolidColorBrush x:Key="BeigeLunchBorder" Color="#E7DAC7" />
            <SolidColorBrush x:Key="BeigeLunchText" Color="#92400E" />
            <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />

            <!-- Global Font: Malgun Gothic for crisp Korean typography -->
            <Style TargetType="{x:Type Window}">
                <Setter Property="FontFamily" Value="Malgun Gothic" />
                <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
                <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
            </Style>

            <!-- 🎯 Universal Bulletproof Button Style: Prevents hover text-disappearing / white-out -->
            <Style TargetType="{x:Type Button}">
                <Setter Property="FontFamily" Value="Malgun Gothic" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter x:Name="contentPresenter"
                                                  Focusable="False"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  Margin="{TemplateBinding Padding}"
                                                  RecognizesAccessKey="True"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.68"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 🎯 Universal Bulletproof ToggleButton Style -->
            <Style TargetType="{x:Type ToggleButton}">
                <Setter Property="FontFamily" Value="Malgun Gothic" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter Focusable="False"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  Margin="{TemplateBinding Padding}"
                                                  RecognizesAccessKey="True"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="#38BDF8"/>
                                    <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 🎯 Universal ComboBox Style: Prevents Korean text vertical clipping -->
            <Style TargetType="{x:Type ComboBox}">
                <Setter Property="FontFamily" Value="Malgun Gothic" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="MinHeight" Value="32" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Padding" Value="8,2,24,2" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
            </Style>

            <Style TargetType="{x:Type ComboBoxItem}">
                <Setter Property="FontFamily" Value="Malgun Gothic" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="MinHeight" Value="28" />
                <Setter Property="Padding" Value="8,4" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
